import { getAllImagesMetadata } from "../../controllers/images.controller"

// jest.mock('../controller/images.controller');

// describe('Unit tests of the images router', () => {
//     it('should return an object with images metadata', async () => {
//         const metadata = await getAllImagesMetadata();
//     })
// })