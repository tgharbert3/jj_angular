export interface pageParam {
    pageNumber: number;
};

export interface contactParam {
    name: String,
    email: String,
    comments: String,
    subscribe: String,
    anime: Boolean,
    arts: Boolean,
    judo: Boolean,
    lang: Boolean,
    sci: Boolean,
    travel: Boolean,
    hear: String,
};